git clone：仓库地址         //clone一个git仓库
git add .               //添加文件到缓存命令
git commit -m "描述"    //提交命令
git push
git checkout            //切换分支命令
git checkout -d         //创建新分支并立即切换到该分支下
git push -u origin 分支名 